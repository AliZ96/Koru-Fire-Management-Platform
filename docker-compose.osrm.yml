services:
  osrm:
    image: osrm/osrm-backend:latest
    container_name: koru_osrm
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./osrm-data:/data
    command: osrm-routed --algorithm mld /data/izmir-latest.osrm
